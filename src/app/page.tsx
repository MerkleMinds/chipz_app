import { HomePage } from "@/components/HomePage";

/**
 * Main Page Component
 * 
 * This server component renders the client-side HomePage component,
 * which uses the new API hooks for data fetching.
 */
export default function Page() {
    return <HomePage />;
}
